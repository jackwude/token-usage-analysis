# 2026-03-04

- 搭建 workspace Git 灾备：已将 `~/.openclaw/workspace` 推送到私有仓库 https://github.com/jackwude/xiaolongxia-backup.git ，并补充 `.gitignore`（避免敏感信息进仓库）。
- 配置 macOS `launchd` 每天凌晨 3 点自动备份 workspace：脚本 `scripts/backup.sh` + `~/Library/LaunchAgents/com.openclaw.backup.plist`，备份日志写入 `logs/backup*.log`。
- 用户选择“备份完成后通知（方案 A + 可开关）”：已创建通知开关文件 `scripts/backup-notify.enabled`，并配置每日 03:05 通知的 cron（由网关 cron 执行，开关文件存在才通知）。
- SpeedCat：已手动完成一次登录与签到（提示“签到成功”，按钮变“明日再来”）；用户要求后续做自动签到方案，并选择用 macOS Keychain 保存账号密码。已用 `security add-generic-password` 写入 Keychain（service: `speedcat.co`，account: `censors.weevil.0a@icloud.com`）。
- 发现自己曾对 SpeedCat “定时任务已配置好”表述不准确：当时只做了开关文件/思路，尚未落地可靠的自动签到定时任务（需要进一步完善浏览器自动化/依赖）。
- 用户反馈回复变慢：定位到重任务（浏览器/cron）+ Telegram 网络 fallback 提示；用户选择 A（不改配置）→ 采取“聊天优先、重任务后台、减少工具调用密度”的策略。
- 用户偏好（需长期生效）：浏览器相关任务结束后采取“激进省内存策略”——默认关闭所有非必要 tab，仅在明确要求保留现场时才保留。已写入 `MEMORY.md` 并推送。
- 用户请求：从 gying.net 做高分电影/电视剧推荐（不要动漫），并增加简介与演员表。
