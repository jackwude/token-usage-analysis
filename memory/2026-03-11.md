### 🟡 黄线操作
- **时间**: 00:00
- **命令**: `python3 -m venv ~/.openclaw/venvs/faster-whisper && ~/.openclaw/venvs/faster-whisper/bin/python -m pip install -U faster-whisper`；并将 `/Users/fx/.agents/skills/faster-whisper/scripts/transcribe` 的 python 改为使用该 venv
- **原因**: 用户要求补齐 faster-whisper 本地方案，以便与 whisper.cpp 做同音频 A/B 测速
- **结果**: 成功，faster_whisper==1.2.1 可用；已完成一次对比测试（同一段 23.1s m4a：whisper.cpp 约 5.99s，faster-whisper 首次运行约 28.31s）

- **时间**: 19:03
- **命令**: `cron update a0a21c97-4d99-4bf8-a992-087c263f96dc`；`cron update f5d2d534-bd3c-44b6-bc74-a7fc1d8f3753`；并手动 `cron run` 验证两条任务
- **原因**: 用户要求检修过去 24 小时失败的定时任务；排查发现 SpeedCat 每日自动签到与每日 12:00 Agent 用量推送均因 agentTurn 超时失败，需要放宽超时并收紧提示词，减少模型改写与拖延
- **结果**: 成功更新两条 cron：后续已进一步切换为 `main/systemEvent` 方案并验证恢复正常；SpeedCat 与 Agent 用量推送当前状态均为 ok。

- **时间**: 20:09
- **命令**: `cron update 20b89d4e-ceaf-406b-b748-22456cb65c8b enabled=false`；并将 `skills/gying-search/scripts/weekly_reco.js`、`weekly_reco.sh` 移到 `skills/gying-search/scripts/_disabled_20260311/`
- **原因**: 用户担心我调试 GYING 每周推荐时误伤 `gying-search` Skill，要求先按保守方案回退并止血
- **结果**: 已禁用 GYING 每周推荐 cron；我新增的实验脚本未删除，已移出可执行路径做备份，原有 `gying-search` 的 `SKILL.md` 未改动。后续按用户要求仅修复了 `search.sh` 的参数传递 bug，并验证恢复可运行。

### GYING / gying-search 排障结论
- **GYING 专用 Profile**：实测登录态正常；首页可见 `收藏 / 观看历史 / 最近更新的电影 / 最近更新的剧集`，之前“Profile 掉登录”的判断属于误判。
- **搜索主链路（已验证）**：顶部真实搜索按钮类名为 `btn-search`；点击搜索后需要**重新扫描所有 `gying.net` 页面上下文**，因为结果页/详情页可能不在原 tab。
- **《寻秦记》案例（已打通）**：已验证可走通“首页搜索 → 搜索结果/详情页 → 详情页中直接提取百度网盘链接”这条主链路；详情页里可直接扫描 `a[href*="pan.baidu.com"]` 抓前 5 条百度链接，且页面同时可见豆瓣评分与网盘分组计数。
- **《剑来 第二季》案例（未完全打通）**：已验证可稳定进入搜索结果页 `https://www.gying.net/s/1---1/%E5%89%91%E6%9D%A5%20%E7%AC%AC%E4%BA%8C%E5%AD%A3`，但这次测试未稳定提取出结果页候选条目；当前最可能的剩余问题是**搜索结果页候选提取规则稳健性不足**，而不是登录/首页/搜索框/搜索按钮问题。
- **Skill 文档已固化**：已把以下实测有效规则写回 `skills/gying-search/SKILL.md` 与 `references/gying-ui.md`：
  1. 搜索时优先点击真实按钮 `btn-search`
  2. 搜索后重新扫描 gying 页面上下文
  3. 详情页优先直接扫描 `pan.baidu.com`，百度优先、夸克兜底
  4. 任务完成后关闭本次新增的非必要 tab，只保留 `gying` 专用 Profile 登录态
- **当前风险提示**：`gying-search` Skill 现在属于“**核心链路已部分恢复，但对不同关键词的稳健性还不够**”；不要再把问题归因为 Profile 掉登录。
