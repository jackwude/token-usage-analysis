
### 负面过滤与分组/合并能力（MCP-only）
- 在 `run_xhs_mcp_detail_export.py` 增加负面过滤参数：`--negative-only`、`--negative-lexicon`、`--max-detail-per-keyword`，并新增导出字段：`neg_flag/neg_score/neg_labels/neg_reasons`。
- 新增词典：`references/negative_lexicon.json`（exclude/signals/strong_patterns/tone 可调）。
- 增强：search_feeds 增加超时与重试：`--search-timeout-ms`(默认90000)、`--search-retry`(默认1)、`--search-retry-sleep`(默认15s)，并区分 `status=empty` 与 `status=failed`。
- 新增：分组计划生成器 `scripts/run_xhs_negative_groups_plan.py`（默认输出到北京时间时间文件夹 `/out/xhs_neg_groups/<YYYYMMDD_HHMMSS>/`，只生成分组文件+打印命令+打印 merge 命令，不执行）。
- 新增：合并脚本 `scripts/merge_xhs_exports.py`（本地合并 JSON→merged.xlsx，按 note_id 去重，附带 source_group/source_file）。

### 运行观察与待修问题
- “正常规则跑前10个关键词”时，子代理实际跑了 `keywords.txt` 全文件（22 个关键词），因为脚本当前不支持 `--limit-keywords`；需要新增参数以便测试/分组时只取前 N 个。
- 运行中出现大量 `mcporter output not json` 错误：`获取Feed详情失败: feed <uuid>#<ts> not found in noteDetailMap`。需要定位 search_feeds 返回的 id 类型（可能包含非 note 的占位/追踪 id），并在 detail 前过滤/改用正确的 note_id 字段。
- 运行过程几乎无中间输出/无 checkpoint，导致无法按 25% 进度回报；建议增加每关键词日志与中间落盘（checkpoint）。
